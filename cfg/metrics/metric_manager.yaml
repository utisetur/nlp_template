metric:
  class_name: torchmetrics.Accuracy
  metric_name: accuracy
  params:
    threshold: 0.5
#    num_classes: ${model.params.n_classes}
#    top_k: 1

other_metrics:
  - class_name: torchmetrics.F1
    metric_name: f1_score
    params:
      average: micro
#      num_classes: ${model.params.n_classes}
